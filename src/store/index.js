import { createStore } from 'vuex';
import addressBalance from './addressBalance';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    addressBalance,
  },
});
